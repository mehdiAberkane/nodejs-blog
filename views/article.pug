extends layout

block content
    .container
        .row.row-offcanvas.row-offcanvas-right
            h1 #{article.title}
            p #{article.content}
            a.btn.btn-danger(href='/article-remove/' + article.slug, role='button') Delete